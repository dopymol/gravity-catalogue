document.addEventListener('htmx:afterSwap',event=>{if(event.detail.target.id==='catalogueContent'){const active=document.querySelector('.brand-pill.active');const brand=active?.dataset.brand||'';document.querySelector('#brandInput').value=brand;const link=document.querySelector('#excelLink');link.href='/export/'+(brand?`?brand=${encodeURIComponent(brand)}`:'');link.textContent=`▣   ${active&&brand?active.querySelector('b').textContent:'Full'} price list (Excel)`}if(event.detail.target.id==='productModal')document.body.classList.add('modal-open')});document.addEventListener('click',event=>{const pill=event.target.closest('.brand-pill');if(pill)document.querySelector('#brandInput').value=pill.dataset.brand||'';if(event.target.closest('[data-close-modal]'))closeModal()});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeModal()});function closeModal(){const modal=document.querySelector('#productModal');if(modal)modal.innerHTML='';document.body.classList.remove('modal-open')}if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('/service-worker.js'));
